"use strict";
let emp1 = { id: 1, name: 'naman' };
let emp2 = { id: 2, name: 'aman' };
function kgToLbs(weight) {
    if (typeof weight === 'number')
        return weight * 2.2;
    else
        return parseInt(weight) * 2.2;
}
let textBox = {
    drag: () => { },
    resize: () => { }
};
let unit = 'cm';
let speed = null;
let ride = {
    speed: speed !== null && speed !== void 0 ? speed : 30
};
//# sourceMappingURL=index.js.map